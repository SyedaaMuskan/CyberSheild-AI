name: cybershield-ai

services:
  backend:
    image: sentinelcode-backend:latest
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env

  frontend:
    image: sentinelcode-frontend:latest
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
